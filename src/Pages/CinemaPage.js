import React from "react";
import CinemaContainer from "../Container/CinemaContainer";

function CinemaPage(props) {
  return (
    <div>
      <CinemaContainer />
    </div>
  );
}

export default CinemaPage;
